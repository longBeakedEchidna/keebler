import React, { Component } from 'react';
import { hot } from 'react-hot-loader';

class CreateRoomDisplay extends Component {
  render() {
    return (
      <div className="CreateRoomDisplay">
        <button ahref="http://localhost:8080/auth/google" />
      </div>
    );
  }
}

export default hot(module)(CreateRoomDisplay);
